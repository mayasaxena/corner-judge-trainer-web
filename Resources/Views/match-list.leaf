#extend("base")

#export("head") {
    <title>Current Matches</title>
    <link rel="stylesheet" href="/styles/match-list.css">
}

#export("body") {
    <div>Join A Match</div>
    <div class="match-buttons">
    #loop(matches, "match") {
        <div class="large button">#(match.match-id)</div>
    }
        <div class="large button">A</div>
        <div class="large button">B</div>
        <div class="large button">C</div>
        <div class="large button">D</div>
    </div>

    <a href="store" class="large white button">Create A New Match</a>
}
